{
  "name": "myday-notion-sync-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test test/worker.test.js",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.26.1"
  }
}
